# PANDA needs no credentials or API keys: it is fully local and offline.
# The only optional setting is where the encrypted vault file lives.
# The default is ~/.panda/vault.db. Uncomment to override (e.g. for tests):
# PANDA_DB_PATH=/absolute/path/to/vault.db
